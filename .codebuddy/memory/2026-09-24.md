# 2026-09-24

## 全家桶开发环境基镜像 (build.Dockerfile)
- `dockerfile/_debian/13/build.Dockerfile` 已整合 Python3.13+uv + Rust stable(rustup) + Node.js(LTS,默认NODE_MAJOR=22)+pnpm(corepack) 三大环境。
- PATH 顺序: `/opt/venv/bin:/usr/local/cargo/bin:${PATH}`；RUSTUP_HOME=/usr/local/rustup，CARGO_HOME=/usr/local/cargo。
- 保留了原 Python 基镜像全部能力（uv 全局配置、C.UTF-8 locale、Asia/Shanghai 时区、crates.io 国内镜像可选）。
- 新建 GitHub Action: `.github/workflows/build-all.yml`（name: build-all），构建 build.Dockerfile，tag 前缀 `13-all-`，推送 ghcr.io/<repo>/debian。
- 说明: `python13.Dockerfile` 与 `rust.Dockerfile` 仍保留为单一语言变体，未删除。
